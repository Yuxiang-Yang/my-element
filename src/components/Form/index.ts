import { withInstall } from '@/utils'
import Form from './Form.vue'
import FormItem from './FormItem.vue'

export const ChForm = withInstall(Form)
export const ChFormItem = withInstall(FormItem)

export * from './types'
