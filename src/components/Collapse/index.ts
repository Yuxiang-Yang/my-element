import { withInstall } from '@/utils'
import Collapse from './Collapse.vue'
import CollapseItem from './CollapseItem.vue'

export const ChCollapse = withInstall(Collapse)
export const ChCollapseItem = withInstall(CollapseItem)

export * from './types'
